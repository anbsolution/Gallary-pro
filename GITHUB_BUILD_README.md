# Gallery Pro V3

GitHub-ready Android source package exported from AI Studio.

Build:
- Debug APK: `./gradlew :app:assembleDebug`
- Unit tests: `./gradlew :app:testDebugUnitTest`

GitHub Actions workflow:
`.github/workflows/build-apk.yml`

The workflow builds the debug APK and uploads it as a GitHub Actions artifact.
